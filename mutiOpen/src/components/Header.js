import React from 'react';

const Header = () => {
  return (
    <div style={{lineHeight:'40px',textAlign:'center'}}>
      Header
    </div>
  );
};

Header.propTypes = {
};

export default Header;
