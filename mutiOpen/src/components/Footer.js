import React from 'react';

const Footer = () => {
  return (
    <div style={{lineHeight:'40px',textAlign:'center'}}>
      Footer
    </div>
  );
};

Footer.propTypes = {
};

export default Footer;
